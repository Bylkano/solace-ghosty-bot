# Copy this file to .env for local runs.
# On Railway: set these in the service Variables tab (do not commit real secrets).

# Discord bot token (required) — either name works.
DISCORD_TOKEN=
# BOT_TOKEN=

BOT_PREFIX=!
OWNER_ID=

# Optional: guild ID for instant slash-command sync while developing.
# Leave blank for global commands (can take up to 1 hour to propagate).
DEV_GUILD_ID=

# PostgreSQL / Neon connection string (required).
# On Railway: paste your Neon URL into Variable DATABASE_URL.
# Neon needs SSL — the bot already uses sslmode=require on every connection.
DATABASE_URL=

# DeepInfra API token (optional unless you use AI features).
DEEPINFRA_TOKEN=
