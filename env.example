# Copy this file to .env and fill in your values.
# Get your bot token from https://discord.com/developers/applications

DISCORD_TOKEN=your_bot_token_here
BOT_PREFIX=!
OWNER_ID=1355644462445101056

# Optional: Guild ID for faster slash command syncing during development.
# Leave blank for global commands (can take up to 1 hour to propagate).
DEV_GUILD_ID=

# PostgreSQL connection string (required).
# On Render: set DATABASE_URL to your Neon connection string (keep it in Render env vars only).
# Neon requires SSL — the bot already uses sslmode=require on every connection.
DATABASE_URL=postgresql://neondb_owner:npg_CQPaHuWm69rZ@ep-fragrant-cherry-ast66qr2.c-4.eu-central-1.aws.neon.tech/neondb?sslmode=require

# DeepInfra API token (required for Biki AI).
# Get yours free at https://deepinfra.com/dash/api_keys
DEEPINFRA_TOKEN=your_deepinfra_token_here
